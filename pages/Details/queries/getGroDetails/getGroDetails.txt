select
  id,
  event_id,
  menu_name,
  ingredient_name,
  required_qty,
  required_unit
from public.gro_details
where client_id = '{{ appsmith.store.current_client_id }}'::uuid
order by
  event_id,
  lower(coalesce(menu_name,'')),
  lower(coalesce(ingredient_name,''));