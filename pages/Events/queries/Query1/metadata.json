{
  "gitSyncId": "6a0a1bde3214f344228346e7_e2dcd642-d6de-4265-90af-4171b873eb6b",
  "id": "Events_Query1",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "select\n    v.id,\n    v.event_id,\n    v.proposal_no,\n    v.proposal_status,\n    v.proposal_title,\n    v.event_name,\n    v.event_ref,\n    v.event_date,\n    v.total_guests,\n    v.sent_at,\n    v.created_at,\n    v.active,\n    v.closed_at,\n    v.was_issued,\n    v.is_editable,\n    v.is_closed,\n    v.proposal_sequence_number,\n    v.proposal_number\nfrom public.vw_prop_selector v\njoin public.prop_items pi\n  on pi.id = v.id\nwhere v.client_id = '{{ appsmith.store.current_client_id }}'::uuid\n  and v.event_id = {{ Number(appsmith.store.current_event_id || 0) }}\n  and coalesce(pi.deleted, false) = false\norder by\n    case\n        when v.proposal_status = 'Ordered' then 0\n        when v.proposal_status = 'Accepted' then 1\n        when v.proposal_status = 'Issued' then 2\n        when v.proposal_status = 'Draft' then 3\n        else 4\n    end,\n    coalesce(v.sent_at, v.created_at) desc,\n    v.id desc;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "FoodCostingDB",
      "isAutoGenerated": false,
      "name": "FoodCostingDB",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "Query1",
    "pageId": "Events",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": false
  }
}