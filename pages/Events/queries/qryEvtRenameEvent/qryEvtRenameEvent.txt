select *
from public.rename_event(
	'{{ appsmith.user.email }}'::text,
	{{ Number(appsmith.store.current_event_id || 0) }},
	'{{ appsmith.store.current_client_id }}'::uuid,
	'{{ (inpEvtRenameName.text || "").trim().replaceAll("'", "''") }}'
);