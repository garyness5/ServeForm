select *
from public.save_mnu_diet_tags_snapshot(
  {{ Number(appsmith.store.current_menu_id || 0) }},
  '{{ JSON.stringify((msEvtDietTags.selectedOptionValues || []).map(id => ({ tag_id: Number(id) }))).replaceAll("'", "''") }}'::jsonb
);