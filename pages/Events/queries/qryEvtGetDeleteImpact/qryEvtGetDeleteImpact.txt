select
    e.id          as event_id,
    e.event_name,
    e.client_id,
    c.client_name
from public.evt_items e
join public.clients c
    on c.id = e.client_id
where coalesce(e.deleted, false) = false
order by
    c.client_name,
    e.id;