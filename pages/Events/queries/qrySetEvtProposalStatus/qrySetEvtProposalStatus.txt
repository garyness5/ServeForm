select *
from public.set_event_proposal_status_secure(
	{{ Number(appsmith.store.proposal_status_id || 0) }},
	'{{ appsmith.store.current_client_id }}'::uuid,
	'{{ String(appsmith.store.proposal_status_request || "").replaceAll("'", "''") }}'
);