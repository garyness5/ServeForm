update public.rec_items
set
  active = {{ this.params.active === true }},
  updated_at = now()
where id = {{ Number(this.params.recipe_id || 0) }}
  and client_id = '{{ appsmith.store.current_client_id }}'::uuid
  and coalesce(deleted, false) = false
returning id, active;