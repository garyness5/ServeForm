select *
from public.update_ingredient_inline(
	{{ Number(this.params.ingredient_id || 0) }},
	'{{ appsmith.store.current_client_id }}'::uuid,
	'{{ JSON.stringify(this.params.changes || {}).replaceAll("'", "''") }}'::jsonb
);