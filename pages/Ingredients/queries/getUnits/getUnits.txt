select
  id,
  name,
  abbreviation,
  unit_type,
  factor_to_base
from public.units
order by
  case unit_type
    when 'weight' then 1
    when 'volume' then 2
    when 'count' then 3
    else 99
  end,
  name;