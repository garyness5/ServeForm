select
  0 as recipe_count,
  0 as dish_count,

  (
		select count(*)
		from public.mnu_components
		where item_type = 'dish'
		and child_dish_id = {{ Number(appsmith.store.current_dish_id || 0) }}
	) as menu_count,

  0 as event_count;