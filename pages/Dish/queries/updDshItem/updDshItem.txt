update public.dsh_items
set
  name = NULLIF('{{ inpDshName.text.trim().replaceAll("'", "''") }}', ''),
  category_id = {{ selDshCategory.selectedOptionValue && selDshCategory.selectedOptionValue !== "" ? Number(selDshCategory.selectedOptionValue) : null }},
  serve_form_id = {{ (selDshFormat.selectedOptionValues || [])[0] ? Number((selDshFormat.selectedOptionValues || [])[0]) : null }},
  yield_qty = {{ inpDshYieldQty.text && inpDshYieldQty.text !== "" ? Number(inpDshYieldQty.text) : null }},
  yield_unit_id = {{ selDshYieldUnit.selectedOptionValue && selDshYieldUnit.selectedOptionValue !== "" ? Number(selDshYieldUnit.selectedOptionValue) : null }},
  extra_percent = {{ inpDshExtraPercent.text && inpDshExtraPercent.text !== "" ? Number(inpDshExtraPercent.text) : 0 }},
  notes = NULLIF('{{ (rteDshNotes.text || "").replaceAll("'", "''") }}', ''),
  active = {{ chkDshActive.isChecked ? true : false }},
  updated_at = now()
where id = {{ Number(appsmith.store.current_dish_id) }}
  and client_id = '{{ appsmith.store.current_client_id }}'::uuid
returning id;