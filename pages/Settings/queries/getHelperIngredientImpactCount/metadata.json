{
  "gitSyncId": "6a0a1bde3214f344228346e7_89b51720-bb6e-42e3-b778-7506cf851f71",
  "id": "Settings_getHelperIngredientImpactCount",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "select\n  case\n    when (\n      select list_code\n      from helper_lists\n      where id = {{ Number(selPicklistType.selectedOptionValue) }}\n    ) = 'supplier'\n      then (\n        select count(*)\n        from ing_items\n        where client_id = '{{ appsmith.store.current_client_id }}'::uuid\n          and supplier_id = {{ Number(tblPicklistItems.selectedRow.id) }}\n      )\n\n    when (\n      select list_code\n      from helper_lists\n      where id = {{ Number(selPicklistType.selectedOptionValue) }}\n    ) = 'packaging'\n      then (\n        select count(*)\n        from ing_items\n        where client_id = '{{ appsmith.store.current_client_id }}'::uuid\n          and packaging_id = {{ Number(tblPicklistItems.selectedRow.id) }}\n      )\n\n    when (\n      select list_code\n      from helper_lists\n      where id = {{ Number(selPicklistType.selectedOptionValue) }}\n    ) = 'ingredient_category'\n      then (\n        select count(*)\n        from ing_items\n        where client_id = '{{ appsmith.store.current_client_id }}'::uuid\n          and category_id = {{ Number(tblPicklistItems.selectedRow.id) }}\n      )\n\n    when (\n      select list_code\n      from helper_lists\n      where id = {{ Number(selPicklistType.selectedOptionValue) }}\n    ) = 'diet_tag'\n      then (\n        select count(distinct idt.ingredient_id)\n        from ing_diet_tags idt\n        join ing_items i\n          on i.id = idt.ingredient_id\n        where i.client_id = '{{ appsmith.store.current_client_id }}'::uuid\n          and idt.helper_list_item_id = {{ Number(tblPicklistItems.selectedRow.id) }}\n      )\n\n    else 0\n  end as ingredient_count;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": true
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "FoodCostingDB",
      "isAutoGenerated": false,
      "name": "FoodCostingDB",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "getHelperIngredientImpactCount",
    "pageId": "Settings",
    "runBehaviour": "AUTOMATIC",
    "userSetOnLoad": false
  }
}