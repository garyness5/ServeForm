select
  id,
  abbreviation,
  name,
  unit_type,
  factor_to_base
from public.units
where active = true
order by
  case lower(unit_type)
    when 'count' then 1
    when 'weight' then 2
    when 'volume' then 3
    else 99
  end,
  sort_order,
  abbreviation;