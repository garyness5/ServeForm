select
  id,
  name,
  active,
  is_system
from helper_list_items
where list_id = {{ Number(selListsType.selectedOptionValue || 0) }}
  and (
    client_id = '{{ appsmith.store.current_client_id }}'::uuid
    or is_system = true
  )
order by lower(name);